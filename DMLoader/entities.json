{ "views": { "all": { "map": "function (doc, meta) {\n  if(doc.Source_User)\n    emit([doc.Source_User, \"User\"]);\n  if(doc.Source_IP)\n    emit([doc.Source_IP, \"Host\"]);\n  if(doc.Destination_IP)\n    emit([doc.Destination_IP, \"Host\"]);\n  if(doc.Application)\n    emit([doc.Application, \"Application\"]);\n  if(doc.Destination_User)\n    emit([doc.Destination_User, \"User\"]);\n  \n}", "reduce": "_count" } } }
